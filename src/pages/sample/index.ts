import * as Store from 'svelte/store'
import {
    // Packages
    Router,
    Components,
    Resources,
} from '../util'

export default {

    // Logics

    // Global Variable
    Components,
    Resources,
    Router,
    Store,
}
