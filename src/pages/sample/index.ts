import * as Store from 'svelte/store'
import {
    // Packages
    Router,
    Resources,
} from '../../utils'

export default {

    // Logics

    // Global Variable
    Resources,
    Router,
    Store,
}
