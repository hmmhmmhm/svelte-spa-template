import * as Store from 'svelte/store'
import {
    // Packages
    Router,
    Resources,
} from '../../utils'

export const canYouSeeMe2 = (test: number) => 'hi??'

export default {
    // Logics
    canYouSeeMe2,

    // Global Variable
    Resources,
    Router,
    Store,
}
